# <%= title %>

## 最后更新时间
<%= time %>

## 剩余文章列表

<% surplus.forEach(li => { %>
- [<%= li.title %>](<%= li.url %>)<% })
-%>

## 全部文章
<% whole.forEach((li,index) => { %>
<%= index + 1 %>. [<%= li.title %>](<%= li.url %>)<% })
-%>
